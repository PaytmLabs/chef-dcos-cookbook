---
driver:
  name: vagrant

provisioner:
  name: chef_zero

verifier:
  name: inspec

platforms:
  - name: centos-7.2

suites:
  - name: default
    run_list:
      - recipe[dcos::default]
    attributes:
      dcos:
        dcos_edition: 'open'
        master_list: ['127.0.0.1']
